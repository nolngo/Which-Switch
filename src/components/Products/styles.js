import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  root: {
    maxWidth: '100%',
  },
  searchBar: {
    display: 'flex',
    justifyContent: 'center',
    marginBottom: 50,
    marginTop: 100
  }
}));
